<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Share Link Generated</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              "retro-orange": "#ff6b35",
              "retro-pink": "#f7931e",
              "retro-purple": "#a663cc",
              "retro-blue": "#4ecdc4",
            },
          },
        },
      };
    </script>
  </head>
  <body
    class="bg-gradient-to-br from-purple-900 via-blue-900 to-indigo-900 min-h-screen text-white font-mono"
  >
    <div class="container mx-auto px-6 py-8">
      <div class="max-w-2xl mx-auto">
        <div class="text-center mb-8">
          <h1
            class="text-3xl font-bold bg-gradient-to-r from-retro-orange to-retro-pink bg-clip-text text-transparent mb-4"
          >
            ✅ Share Link Generated!
          </h1>
        </div>

        <div
          class="bg-white/10 backdrop-blur-lg rounded-2xl p-8 shadow-2xl border border-white/20"
        >
          <div class="text-center mb-6">
            <div class="text-4xl mb-4">🎉</div>
            <p class="text-gray-300 mb-4">
              Your folder has been shared successfully! The link will expire in
              <strong><%= duration %> day(s)</strong>.
            </p>
            <p class="text-sm text-gray-400">
              Expires: <%= expiresAt.toLocaleString() %>
            </p>
          </div>

          <div class="mb-6">
            <label class="block text-sm font-medium text-gray-300 mb-2">
              Share this link:
            </label>
            <div class="flex gap-2">
              <input
                type="text"
                readonly
                value="<%= shareUrl %>"
                id="shareUrl"
                class="flex-1 px-4 py-3 bg-white/10 border border-white/20 rounded-lg text-white focus:outline-none focus:ring-2 focus:ring-retro-blue"
              />
              <button
                onclick="copyToClipboard()"
                class="px-4 py-3 bg-gradient-to-r from-retro-blue to-retro-purple rounded-lg font-semibold text-white hover:shadow-lg transition-all duration-300 hover:scale-105"
              >
                📋 Copy
              </button>
            </div>
          </div>

          <div class="text-center">
            <a
              href="/"
              class="inline-block px-6 py-3 bg-gradient-to-r from-gray-600 to-gray-700 rounded-lg font-semibold text-white hover:shadow-lg transition-all duration-300 hover:scale-105"
            >
              ← Back to Home
            </a>
          </div>
        </div>
      </div>
    </div>

    <script>
      function copyToClipboard() {
        const shareUrl = document.getElementById("shareUrl");
        shareUrl.select();
        shareUrl.setSelectionRange(0, 99999);
        navigator.clipboard.writeText(shareUrl.value);

        const button = event.target;
        const originalText = button.textContent;
        button.textContent = "✅ Copied!";
        button.classList.add("bg-green-600");

        setTimeout(() => {
          button.textContent = originalText;
          button.classList.remove("bg-green-600");
        }, 2000);
      }
    </script>
  </body>
</html>
