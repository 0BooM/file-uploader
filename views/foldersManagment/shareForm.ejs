<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Share Folder</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              "retro-orange": "#ff6b35",
              "retro-pink": "#f7931e",
              "retro-purple": "#a663cc",
              "retro-blue": "#4ecdc4",
            },
          },
        },
      };
    </script>
  </head>
  <body
    class="bg-gradient-to-br from-purple-900 via-blue-900 to-indigo-900 min-h-screen text-white font-mono"
  >
    <div class="container mx-auto px-6 py-8">
      <div class="max-w-md mx-auto">
        <div class="text-center mb-8">
          <h1
            class="text-3xl font-bold bg-gradient-to-r from-retro-orange to-retro-pink bg-clip-text text-transparent mb-4"
          >
            🔗 Share Folder
          </h1>
        </div>

        <div
          class="bg-white/10 backdrop-blur-lg rounded-2xl p-8 shadow-2xl border border-white/20"
        >
          <% if (error) { %>
          <div
            class="mb-4 p-3 bg-red-500/20 border border-red-500/30 rounded-lg text-red-200"
          >
            <%= error %>
          </div>
          <% } %>

          <form method="POST" action="/folder/<%= folderId %>/share">
            <div class="mb-6">
              <label
                for="duration"
                class="block text-sm font-medium text-gray-300 mb-2"
              >
                Share duration (days):
              </label>
              <input
                type="number"
                id="duration"
                name="duration"
                min="1"
                max="365"
                required
                class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-lg text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-retro-blue focus:border-transparent"
                placeholder="Enter days (e.g., 1, 7, 30)"
              />
              <p class="text-xs text-gray-400 mt-1">
                How long should this share link be valid?
              </p>
            </div>

            <div class="flex gap-3">
              <button
                type="submit"
                class="flex-1 py-3 bg-gradient-to-r from-retro-orange to-retro-pink rounded-lg font-semibold text-white hover:shadow-lg transition-all duration-300 hover:scale-105"
              >
                🔗 Generate Share Link
              </button>
              <a
                href="/folder/<%= folderId %>"
                class="px-4 py-3 bg-gray-600/50 rounded-lg font-semibold text-white hover:bg-gray-600/70 transition-colors"
              >
                Cancel
              </a>
            </div>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
